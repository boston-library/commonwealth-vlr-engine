<%# override so we can add custom dropdowns for facets %>
<div id="sortAndPerPage" class="sort-pagination d-md-flex justify-content-between" role="navigation"
     aria-label="<%= t('blacklight.search.per_page.aria_label')%>">
  <%= render partial: "paginate_compact", object: @response if show_pagination? %>
  <%= render partial: 'collections_facet_dropdowns' %>
  <%= render_results_collection_tools wrapping_class: "search-widgets" %>
</div>
