<%# if current_user %>
<% if current_or_guest_user %>
  <%= link_to t('blacklight.saved_searches.save'),
              save_search_path(session[:history].first),
              method: :put,
              id: 'saveSearchLink',
              class: 'catalog_saveSearchLink btn btn-sm btn-outline-dark' %>
<% end %>