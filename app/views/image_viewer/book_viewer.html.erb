<%# DEPRECATED, currently unused, prefer CommonwealthVlrEngine::Media::BookViewerComponent instead %>
<% @page_title = render_title(@document, false) + ' - ' + application_name %>

<%= render partial: 'uv_header' %>

<%= render partial: 'uv_viewer' %>
